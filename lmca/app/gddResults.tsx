import React from 'react';
import { useRouter } from 'expo-router'; // Adjust import if necessary
import { Text, View, StyleSheet } from 'react-native';
import Body from "@/components/Body";

export default function gddResults () {


  return (
    <Body>
      
    </Body>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
    margin: 10,
  }
});
